#include "main.h"

/**
 * print_square - function to lower case checker
 * @size: number of '#'
*/

void print_square(int size)
{
	if (size <= 0)
	{
		_putchar('\n');
	}
	else
	{
		for (int i = 1 ; i < size ; i++)
		{
			for (j = 1 ; j < zise ; j++)
			{
				_putchar(35);
			}
			_putchar('\n');
		}
	}
}
